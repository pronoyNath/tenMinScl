

const TransparentArrowButton = ({ className = "" }) => {
  return (
    <div
      className={ `!text-gray-400  !bg-white  rounded-full  border-white  cursor-pointer ${className}`}
    >
      
    </div>
  );
};

export default TransparentArrowButton;
